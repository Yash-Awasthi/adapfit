# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9

# HTTP client (for Gemini API calls)
httpx>=0.27.0

# Rate limiting
slowapi>=0.1.9

# Database (Supabase Postgres)
supabase>=2.0.0
asyncpg>=0.30.0
pgvector>=0.3.0

# ML/AI (optional - app works without these via rule-based fallbacks)
torch>=2.0.0
xgboost>=2.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
mediapipe==0.10.21
opencv-contrib-python<4.10
Pillow>=10.0.0

# NLP (optional - falls back to keyword-based sentiment)
# transformers>=4.30.0
# sentence-transformers>=2.2.0

# Vector search (optional - falls back to cosine similarity)
# qdrant-client>=1.6.0

# Structured logging
structlog>=23.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.21.0
yt-dlp>=2026.8.19
